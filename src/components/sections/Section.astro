---
const { id, class: className = "" } = Astro.props;
---

<section
  id={id}
  class={`relative px-6 md:px-10 lg:px-0 py-24 md:py-32 ${className}`}
>

  <div class="w-full max-w-[1280px] mx-auto">
    <slot />
  </div>

</section>